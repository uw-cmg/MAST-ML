----- Folder contents -----
per_group_info.png
    Overlayed plot.
    See containing folder's README for more detail.
data_{series}.csv
    Data for each series in the plot:
    x data column
    x error column (all zeros is no error)
    y data column, labeled as the series in the plot
