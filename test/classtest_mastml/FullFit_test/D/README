----- Folder contents -----
output_data.csv:
    Output data, consisting of:
    Labeling features columns, if any
    Input features columns
    Target feature column
    Target prediction column
statistics.txt:
    Statistics for the fit.
{y} vs {x}.png:
    Plot of predicted vs. measured data
data_ .csv
    Plotted data, in csv format
    Measured data column
    Measured data error column (all zeros is no error)
    Predicted data column
    Predicted data error column (all zeros is no error)
